<UserControl x:Class="CugaCalibration.Views.Common.Windows.Recipe.Edit.WaferMap.Children.WaferMapCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wpf="clr-namespace:Net.Utilities.WaferMap.WPF;assembly=Net.Utilities.WaferMap.WPF"
             xmlns:attachedHelper="clr-namespace:Net.Utilities.WPF.AttachedHelper;assembly=Net.Utilities.WPF"
             xmlns:children1="clr-namespace:CugaCalibration.ViewModels.Common.Windows.Recipe.Edit.Children"
             d:DataContext="{d:DesignInstance Type=children1:RecipeWaferMapViewModel,
                                              IsDesignTimeCreatable=True}"
             d:DesignHeight="1200"
             d:DesignWidth="1200"
             mc:Ignorable="d">
    <UserControl.Resources>
        <attachedHelper:BindingProxy x:Key="BindingProxy" Value="{Binding}" />
    </UserControl.Resources>
    <Grid>
        <Border Padding="2"
                Style="{StaticResource BorderRegion}"
                Effect="{StaticResource EffectShadow1}">
            <wpf:WaferMapCanvas x:Name="WaferMapCanvas" Grid.Row="2"
                                DataContext="{Binding WaferMapCanvasViewModel}">
                <wpf:WaferMapCanvas.ContextMenu>
                    <ContextMenu>
                        <MenuItem Command="{Binding Path=Value.(children1:RecipeWaferMapViewModel.GotoPositionCommand), Source={StaticResource BindingProxy}}" Header="Goto Position" />
                        <!--  可以根据需要添加更多菜单项  -->
                    </ContextMenu>
                </wpf:WaferMapCanvas.ContextMenu>
            </wpf:WaferMapCanvas>
        </Border>
    </Grid>
</UserControl>
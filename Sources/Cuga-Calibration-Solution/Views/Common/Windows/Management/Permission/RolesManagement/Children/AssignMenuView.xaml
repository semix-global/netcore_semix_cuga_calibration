<UserControl x:Class="CugaCalibration.Views.Common.Windows.Management.Permission.RolesManagement.Children.AssignMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:CugaCalibration.ViewModels.Common.Windows.Management.Permission"
             xmlns:controls="clr-namespace:WPF.CustomControl.UI.PermissionMenuTreeView;assembly=WPF.CustomControl"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             d:DataContext="{d:DesignInstance Type=viewModels:RolesManagementViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid>
        <Border Padding="2" Style="{StaticResource BorderRegion}">
            <hc:UniformSpacingPanel Grid.ColumnSpan="2"
                                    Margin="5,5,5,5"
                                    VerticalAlignment="Top"
                                    Orientation="Vertical"
                                    Spacing="5">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <controls:PermissionMenuTreeView HorizontalAlignment="Left"
                                                     VerticalAlignment="Top"
                                                     IsCheckBoxEnabled="{Binding OperateCommandIndex}"
                                                     ItemsSource="{Binding MenuAllocationTreeList}" />
                </ScrollViewer>
            </hc:UniformSpacingPanel>
        </Border>
    </Grid>
</UserControl>
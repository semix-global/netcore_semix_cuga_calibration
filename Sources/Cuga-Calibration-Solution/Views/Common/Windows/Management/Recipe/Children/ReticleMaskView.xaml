<UserControl x:Class="CugaCalibration.Views.Common.Windows.Management.Recipe.Children.ReticleMaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:wpf="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:behaviorsCore="clr-namespace:CugaCalibration.Core.Behaviors"
             xmlns:viewModels="clr-namespace:CugaCalibration.ViewModels.Common.Windows.Management.Recipe"
             d:DataContext="{d:DesignInstance Type=viewModels:RecipeSettingViewModel}"
             d:DesignHeight="900"
             d:DesignWidth="400"
             mc:Ignorable="d">
    <Grid>
        <Border Padding="2"
                Style="{StaticResource BorderRegion}"
                Effect="{StaticResource EffectShadow1}">
            <wpf:WpfPlot Width="{Binding Width, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}" Height="{Binding Height, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}">
                <i:Interaction.Behaviors>
                    <behaviorsCore:WpfPlotReticleMaskBehavior DieSize="{Binding WaferMapCanvasViewModel.Document.DieBuilder.DieSize}"
                                                              ReticleMaskList="{Binding ReticleMarkList}"
                                                              WaferCenterBrightFieldPosition="{Binding CalibrationRecipeDto.WaferDto.WaferCenterWaferPosition}" />
                </i:Interaction.Behaviors>
            </wpf:WpfPlot>
        </Border>
    </Grid>
</UserControl>
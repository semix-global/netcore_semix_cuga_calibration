<UserControl x:Class="CugaCalibration.Views.Common.Windows.File.Setting.Children.SettingCalibrateItemsStatusUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:CugaCalibration.ViewModels.Common.Windows.File.Setting.Children"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             d:DataContext="{d:DesignInstance Type=viewModels:SettingCalibrateItemsStatusViewModel}"
             d:DesignHeight="800"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ControlTemplate x:Key="CustomItemsControlTemplate" TargetType="ItemsControl">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsPresenter />
            </ScrollViewer>
        </ControlTemplate>
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Border>
        <Grid>
            <Grid.Resources>
                <Style BasedOn="{StaticResource GroupBoxBaseStyle}" TargetType="GroupBox">
                    <Style.Setters>
                        <Setter Property="Margin" Value="5" />
                        <Setter Property="Padding" Value="10" />
                    </Style.Setters>
                </Style>
            </Grid.Resources>

            <!--  使用ItemsControl展示所有类别  -->
            <ItemsControl ItemsSource="{Binding CalibrationCategories}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="4" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!--  每个类别显示为一个GroupBox  -->
                        <GroupBox Header="{Binding Description}">
                            <hc:UniformSpacingPanel Orientation="Vertical" Spacing="5">
                                <!--  类别内的项目使用ItemsControl显示  -->
                                <ItemsControl ItemsSource="{Binding Items}" Template="{StaticResource CustomItemsControlTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter" />
                                    </ItemsControl.ItemContainerStyle>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <hc:SimplePanel Height="30" Margin="8">
                                                <TextBlock Text="{Binding Path=Description}"
                                                           MinWidth="300"
                                                           HorizontalAlignment="Left"
                                                           VerticalAlignment="Center" />
                                                <ToggleButton IsChecked="{Binding IsAnyOk}"
                                                              HorizontalAlignment="Right"
                                                              VerticalAlignment="Center"
                                                              Background="{StaticResource DangerBrush}"
                                                              IsEnabled="{Binding IsAnyOk}"
                                                              Style="{StaticResource ToggleButtonSwitch}" />
                                            </hc:SimplePanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </hc:UniformSpacingPanel>
                        </GroupBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
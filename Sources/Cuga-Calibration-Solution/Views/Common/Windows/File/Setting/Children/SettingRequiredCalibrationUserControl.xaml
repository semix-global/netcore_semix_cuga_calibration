<UserControl x:Class="CugaCalibration.Views.Common.Windows.File.Setting.Children.SettingRequiredCalibrationUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:cookiesUi="clr-namespace:Core.Models.Models.Common.Cookies.UI;assembly=Core.Models"
             xmlns:viewModels="clr-namespace:CugaCalibration.ViewModels.Common.Windows.File.Setting.Children"
             d:DataContext="{d:DesignInstance Type=viewModels:SettingRequiredCalibrationViewModel}"
             d:DesignHeight="800"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <cookiesUi:CalibrationTreeView Margin="5"
                                   CheckBoxStyle="{StaticResource CheckBoxBaseStyle}"
                                   ItemsSource="{Binding SettingRequiredCalibrationParamList}" />
</UserControl>
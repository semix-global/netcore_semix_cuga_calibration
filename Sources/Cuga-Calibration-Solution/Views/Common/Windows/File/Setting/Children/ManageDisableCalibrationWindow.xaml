<hc:Window x:Class="CugaCalibration.Views.Common.Windows.File.Setting.Children.ManageDisableCalibrationWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:hc="https://handyorg.github.io/handycontrol"
           xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           xmlns:viewModels="clr-namespace:CugaCalibration.ViewModels.Common.Windows.File.Setting.Children"
           Width="600"
           Height="500"
           d:DataContext="{d:DesignInstance Type=viewModels:ManageDisableCalibrationWindowViewModel}"
           BorderBrush="{StaticResource PrimaryBrush}"
           BorderThickness="2"
           ResizeMode="NoResize"
           ShowInTaskbar="False"
           WindowStartupLocation="CenterScreen"
           WindowStyle="None"
           mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  标题栏  -->
        <hc:SimplePanel Grid.Row="0" Background="{StaticResource PrimaryBrush}">
            <StackPanel Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Grid}, Path=Width}"
                        VerticalAlignment="Center"
                        Background="Transparent">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                        <i:CallMethodAction MethodName="DragMove" TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <TextBlock Text="Manage Disable Calibration Configurations"
                           HorizontalAlignment="Center"
                           FontSize="13"
                           FontWeight="Bold"
                           Foreground="White"
                           Style="{StaticResource TextBlockDefault}" />
            </StackPanel>
            <Button Command="{Binding CloseDialogCommand}"
                    HorizontalAlignment="Right"
                    hc:IconElement.Geometry="{StaticResource ErrorGeometry}"
                    Cursor="Hand">
                <Button.Style>
                    <Style BasedOn="{StaticResource ButtonIcon}" TargetType="Button">
                        <Setter Property="Foreground" Value="White" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </hc:SimplePanel>

        <!--  主体内容  -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  配置列表  -->
            <Border Grid.Row="0"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1">
                <DataGrid hc:DataGridAttach.ShowRowNumber="True"
                          ItemsSource="{Binding SettingDisableCalibrationConfigList}"
                          SelectedItem="{Binding ConfigItem}"
                          Style="{StaticResource DataGrid.Small}"
                          HeadersVisibility="All"
                          RowHeaderWidth="60"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="*"
                                            Header="Configuration Name"
                                            Binding="{Binding ConfigDescription, UpdateSourceTrigger=PropertyChanged}"
                                            FontSize="12">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="5" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                            <DataGridTextColumn.EditingElementStyle>
                                <Style TargetType="TextBox">
                                    <Setter Property="Margin" Value="5" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                </Style>
                            </DataGridTextColumn.EditingElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!--  增删按钮  -->
            <hc:UniformSpacingPanel Grid.Row="1"
                                    Margin="0,10,0,0"
                                    HorizontalAlignment="Left"
                                    Orientation="Horizontal"
                                    Spacing="10">
                <Button Content="Add"
                        Command="{Binding AddCommand}"
                        Width="80"
                        Style="{StaticResource ButtonSuccess}" />
                <Button Content="Remove"
                        Command="{Binding RemoveCommand}"
                        Width="80"
                        Style="{StaticResource ButtonDanger}" />
                <Button Content="Edit"
                        Command="{Binding EditCommand}"
                        Width="100"
                        HorizontalAlignment="Right"
                        Style="{StaticResource ButtonWarning}" />
            </hc:UniformSpacingPanel>
        </Grid>

        <!--  底部按钮  -->
        <hc:SimplePanel Grid.Row="2" Margin="10">
            <Button Content="Save"
                    Command="{Binding SaveCommand}"
                    Width="100"
                    HorizontalAlignment="Right"
                    Style="{StaticResource ButtonPrimary}" />

        </hc:SimplePanel>
    </Grid>
</hc:Window>
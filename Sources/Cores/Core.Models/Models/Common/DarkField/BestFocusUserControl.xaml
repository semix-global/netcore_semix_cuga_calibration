<UserControl x:Class="Core.Models.Models.Common.DarkField.BestFocusUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:darkField="clr-namespace:Core.Models.Models.Common.DarkField"
             xmlns:wpf="clr-namespace:Core.Utilities.WPF;assembly=Core.Utilities.WPF"
             d:DataContext="{d:DesignInstance Type=darkField:BestFocus}"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ScrollViewer MaxHeight="30"
                      Margin="10,2"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <TextBlock TextWrapping="Wrap">
                <Hyperlink Command="{x:Static wpf:Commands.OpenFilePathCommand}" CommandParameter="{Binding RawImageFilePath}">
                    <Run Text="{Binding RawImageFilePath}" />
                </Hyperlink>
            </TextBlock>
        </ScrollViewer>
        <TabControl Grid.Row="1">
            <TabItem Header="Y Strehl Ratio">
                <Border Margin="5" Style="{StaticResource BorderRegion}">
                    <ContentControl Content="{Binding YStrehlRatioScatterPlotControl, Mode=OneWay}" />
                </Border>
            </TabItem>
            <TabItem Header="X Strehl Ratio">
                <Border Margin="5" Style="{StaticResource BorderRegion}">
                    <ContentControl Content="{Binding XStrehlRatioScatterPlotControl, Mode=OneWay}" />
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
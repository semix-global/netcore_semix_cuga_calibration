<Window x:Class="AlgorithmTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MainWindow"
        Width="800"
        Height="450"
        mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Margin" Value="5" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0"
                Content="SavitzkyGolayFilter"
                Click="ButtonSavitzkyGolayFilterOnClick" />
        <Button Grid.Row="1"
                Content="AutomaticMPeakDetection"
                Click="ButtonAutomaticMPeakDetectionOnClick" />
        <Button Grid.Row="2"
                Content="MovMean"
                Click="ButtonOnMovMeanClick" />
        <Button Grid.Row="3"
                Content="RawSplit"
                Click="ButtonOnRawSplitClick" />
    </Grid>
</Window>
<Project>

    <PropertyGroup>
        <RepositoryDirectory>$([System.IO.Path]::GetDirectoryName($(MSBuildThisFileDirectory.TrimEnd('\'))))</RepositoryDirectory>
        <BuildToolsDirectory>$(RepositoryDirectory)\Build</BuildToolsDirectory>
        <SolutionDirectory>$(SolutionDir.TrimEnd('\'))</SolutionDirectory>
    </PropertyGroup>

    <Import Project="$(BuildToolsDirectory)\Common.Build.props" />

    <PropertyGroup>
        <Version>2.5.0320</Version>
        <AssemblyVersion>2.5.1.0320</AssemblyVersion>
        <FileVersion>2.5.1.0320</FileVersion>
    </PropertyGroup>

    <ItemGroup>
        <KeepLanguageAssemblies Include="zh-Hans" />
    </ItemGroup>

</Project>